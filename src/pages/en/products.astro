---
import Base from '../../layouts/Base.astro';

const products = [
  {
    name: 'agentflow',
    note: 'A visual org chart editor built for OpenClaw multi-agent systems.',
    links: [
      {
        label: 'dev.to',
        href: 'https://dev.to/mtdnot_9442010c0f26a0df93/i-built-a-visual-org-chart-editor-for-openclaw-multi-agent-systems-om7',
      },
    ],
  },
];
---

<Base title="Products">
  <h1>Products</h1>
  <p>A page for collecting apps and products that are publicly available.</p>

  {products.map((product) => (
    <section>
      <h2>{product.name}</h2>
      <p>{product.note}</p>
      <p>
        {product.links.map((link) => (
          <a href={link.href} target="_blank" rel="noopener noreferrer">
            {link.label}
          </a>
        ))}
      </p>
    </section>
  ))}
</Base>
