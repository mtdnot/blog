{
  "name": "mtdnot-blog",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler pages deploy dist"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.0",
    "astro": "^5.0.0",
    "marked": "^17.0.5"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
